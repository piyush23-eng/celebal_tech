# Week 2 — ShopEase E-commerce Database Analysis

## 🎯 Objective

Build SQL fundamentals by designing and querying a small e-commerce database (`ShopEase`) — covering the basic query patterns (filtering, aggregation, joins) that get extended into subqueries/CTEs/window functions in Week 3 and the full 16-query analysis in Week 8.

## 🗂️ Database Overview

`ShopEase` models a simple online store — customers, products, and orders (adjust this section to match the actual tables/schema you built).

## 🛠️ Steps Covered

1. **Schema design** — defined tables and relationships (customers, products, orders, etc.)
2. **Data loading** — populated tables with sample data
3. **Basic SELECT queries** — filtering with `WHERE`, sorting with `ORDER BY`
4. **Aggregations** — `COUNT`, `SUM`, `AVG`, `GROUP BY` for order/revenue summaries
5. **Joins** — combining customer, product, and order data across tables
6. **Business questions answered via SQL** — e.g. top-selling products, customer order counts, revenue by category

## 📈 Key Insights

<!-- Fill in your actual query results, e.g.:
- Top-selling product: [name], N units sold
- Total revenue across all orders: $X
- Customer with highest order count: [name], N orders
-->

## 📁 Output

- SQL script(s) with all queries
- Query results / screenshots
- Brief summary of findings

## 🔧 Tech Used

SQL (SQLite / MySQL)
